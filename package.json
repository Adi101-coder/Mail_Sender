{
  "name": "gmail-bulk-mail-sender",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,client -c blue,green \"npm run dev -w backend\" \"npm run dev -w client\"",
    "build": "npm run build -w backend && npm run build -w client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
