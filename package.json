{
  "name": "gmail-bulk-mail-sender",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,client -c blue,green \"npm run dev -w backend\" \"wait-on -t 60000 http://localhost:3001/api/health && npm run dev -w client\"",
    "dev:backend": "npm run dev -w backend",
    "dev:client": "npm run dev -w client",
    "dev:clean": "npx --yes kill-port 3001 5173 && npm run dev",
    "build": "npm run build -w backend && npm run build -w client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.3"
  }
}
