/**
 * Template module — campaign subject/body act as the email template in Phase 1.
 * Template CRUD is handled via the Campaign module.
 */
export { createCampaign, getCampaignById } from '../campaign/campaign.service.js'
